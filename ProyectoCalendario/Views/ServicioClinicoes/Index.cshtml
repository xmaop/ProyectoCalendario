@model IEnumerable<ProyectoCalendario.Models.ServicioClinico>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ListaAtencionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CalendarioId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.detalleCalendarioId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.pautasId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FF)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ListaAtencionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CalendarioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.detalleCalendarioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pautasId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FF)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ServicioClinicoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ServicioClinicoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ServicioClinicoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
